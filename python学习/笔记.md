```python
1，条件判断
	if(条件):
        print("if")
     else:
        print("else")
2,循环语句
	while 条件 and 条件:
        print("while")
```

工具类

```python
1，输出任意0-10整数
	random.randint(0,10)
2，str():转成字符串 ，int():转成整型 ，float()：转成浮点
3，判断数据类型
	type(1)-->直接返回类型;或者isinstance("a",str)-->true
4，预算符号
	**：幂次运算 2**3 （2到3次方） //：除号得到小数点以后会舍弃
    + - * /  加减乘除
5，逻辑运算
	not and or
```

